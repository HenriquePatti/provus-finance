/**
 * Popula o banco de dados com dados iniciais de desenvolvimento.
 *
 * Fase 1 — Categorias padrão:
 * As 13 categorias padrão (9 despesa + 4 receita) são inseridas
 * automaticamente pela migration 003_create_categorias.sql (RK-006).
 * Não é necessário seed separado para esses dados.
 *
 * Este script está preparado para fases futuras caso dados
 * adicionais de desenvolvimento sejam necessários.
 */

console.log('');
console.log('🌱 Seeds — Fase 1:');
console.log('   Categorias padrão: populadas via migration 003_create_categorias.sql (13 categorias).');
console.log('   Nenhum seed adicional necessário nesta fase.');
console.log('');
